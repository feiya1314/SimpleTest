package mytest.structures.Queue;

import java.util.PriorityQueue;

public class HeapPriorityQueue<T extends Comparable<T>> {
    private class Heap<T>{
        PriorityQueue<T> t;
        private T node[];
        boolean insert(T node){
            return false;
        }
    }
}
